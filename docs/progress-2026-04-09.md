# smolclaw Progress Report — 2026-04-09

## Summary

Custom provider support, fallback provider routing fix, adaptive timeout, and auto .gitignore. Enables Ollama cloud as a named provider for flat-rate inference.

## Changes (4 commits)

- **Custom providers** (9eebac4): Config parser handles arbitrary named providers beyond the builtin 10. Factory resolves custom providers by name. Doctor checks connectivity for custom providers.
- **Fallback routing** (dcb5d34): Fallback models now use the same provider as the primary when a custom provider is active. Previously `qwen3.5:397b` on `ollama-cloud` was auto-routed to local `ollama` → 404.
- **Adaptive timeout** (48ae656): Transient LLM failures (HTTP 0/429/502/503) add 30s grace per failure, capped at 300s. Connection failures consume zero tokens — waiting costs nothing.
- **Auto .gitignore** (c466980): After git clone, populates `.git/info/exclude` with workspace artifact patterns (audit.log, state/, tee/, build/).

## Validation

- 26/26 tests pass
- Ollama cloud provider validated: `gemma4:31b via ollama-cloud` confirmed in logs
- GLM-5.1 tool calling works on Ollama cloud (empty on plain chat due to thinking mode)
