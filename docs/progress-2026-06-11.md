# Progress 2026-06-09 → 2026-06-11

## Summary

Three-day arc closing the scope plan:
researcher-contamination class closed end-to-end, agent push access
secured, the camera agent flagship shipped and verified live, the
"smol" size claim turned into a CI regression test (catching real
creep on day one), and the cost-reporting path consolidated onto a
single truthful pricing function.

## Changes (chronological by commit)

- `acbfaf7` fix(context): gate scratchpad/action-log injection on
  session isolation (Layer A of the 2026-06-05 contamination
  diagnosis) + 2 tests in `tests/test_context_isolation.c`.
- `38f73c9` docs: SCOPE.md — goals/non-goals; smolclaw is the product,
  embedded-first, new features land default-off, CI size budget rule.
- `7fcb314` fix(memory): Layer B.3 — both consolidation prompts exclude
  per-run state; bullet-line filter in `agent_session.c` blocks model
  monologue from reaching daily notes; `memory_compact.c` "Current
  State" section removed.
- `1ae83cd` fix(agent): per-turn registry denylist (`registry.c`
  is_allowed) hides + blocks `memory_*`/`note` tools in isolated turns
  (Layer A secondary).
- `64d0f65` fix(git): push deny-by-default — no
  `git.push_allowed_remotes` means no push; 3 new tests.
- `4bbf539` feat(camera): camera tool (`SC_ENABLE_CAMERA`, default n):
  snap / events / describe (base64 → ollama vision `/api/chat`),
  workspace-confined, tests against mock ollama.
- `c998ae9` fix(kconfig): add SC_ENABLE_CAMERA to
  `scripts/kconfig_genconfig.py` FEATURE_SYMS (hardcoded list silently
  drops unknown flags — bites every new flag).
- `b9543e4` feat(web): inline image attachments (`/api/media`,
  `attachments[]`) + live progress feed (`/api/progress`, provider
  named per step). `eca6184` fix(web): expand `~` in workspace.
- `9f13ac4` feat(web): optional live-stream embed
  (`channels.web.embed_stream_url`, authed `/api/ui-config`).
- `f47277d` docs: armv7 CLI exit SIGILL known issue (gateway
  unaffected).
- `f5aa5f9` feat(build): CI size-budget job
  (`scripts/check_size_budget.sh`, minimal ≤ 320 KB stripped). Caught
  real creep: minimal had grown to 1121 KB via (a) rotted
  `configs/defconfig.minimal` missing six newer default-y flags and
  (b) `tools/host.c` unconditional SQLite (~700 KB) — now gated behind
  new `SC_ENABLE_HOST_METRICS` (host_status/inventory unaffected,
  record stubs to no-op, host_trend errors cleanly). Also fixed host.c
  never including `sc_features.h`.
- `341faef` feat(kconfig): voice + code_graph default n (Phase 3b).
- `5e894cb` fix(cost): `report_cost_to_smolchat` used a private rate
  table with a paid fallback — phantom dollars for every local-ollama
  turn. New `sc_cost_tracker_estimate()` is the single pricing path;
  posts prefer provider-reported actuals.
- `160b4e0` docs: tracked May design docs (master plan, phases, etc.).

## Validation

- Full suite green throughout: **37/37** (camera-enabled variant
  35/35 including `test_camera`).
- Minimal build: **257 KB stripped** (budget 320 KB, headroom 63 KB) —
  MinSizeRel + `-ffunction-sections -fdata-sections -Wl,--gc-sections`.
- Isolation re-baseline post-deploy: **3/3 clean runs, plus a 4th
  confirming run after the tool denylist**.
- Camera agent verified end-to-end through a live gateway
  (snap → remote vision model → description + inline photo).

## Remaining / next steps

- armv7 CLI one-shot exit SIGILL
  (`docs/known-issue-armv7-cli-exit-sigill.md`) — gateway unaffected.
- `collect_attachments` hardcodes `camera/` dirs in web.c — refactor
  to tool-declared attachments (`sc_tool_result_t` field) next time
  web.c is touched. Token-streaming of replies via the progress poll:
  designed, not built.
- Web bearer-token UX (raw 64-hex prompt) needs a friendlier flow.
- Pre-existing benign warning: implicit `asprintf` in main.c:1788
  (int-returning, no truncation risk; needs `_GNU_SOURCE`).
