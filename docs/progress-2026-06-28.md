# Progress — 2026-06-28 (Phase 3 completed, Phase 4 mostly landed)

## Summary

Completed **Phase 3** (silent delivery tokens, notify Slack/ntfy, Signal channel
MVP) and landed **ten of eleven** Phase 4 tasks. All work followed the
slice-by-slice protocol: one task → one branch → gated by clean build (KC-2),
`ctest`, and the ≤1024 KB minimal size budget → ff-merge to `master` → push to
Gitea only. HEAD is `481a84e`. The only remaining Phase 4 READY task is **4.1**
(arena adoption in the provider parse path); 4.3/4.6/4.9 are GATED-EXT
(human/external acceptance).

## Changes

### Phase 3 (completed)
- **3.9 silent delivery tokens** (`19e2695`) — `sc_gateway_is_silent_token()` in
  `src/gateway_route.h`; gateway suppresses delivery of `[SILENT]`/`SILENT`/
  `NO_REPLY`/`NO REPLY` (whole-response match, trimmed+case-folded). Top-level
  `silent_tokens_enabled` (default true). Transcript/errors free by construction.
- **3.5 notify Slack + ntfy** (`c241be5`) — `slack://` + `ntfy://` schemes in
  `src/tools/notify.c`; pure parser extracted to `notify_internal.h` with
  `test_notify.c` (retro-covers discord/tg/json).
- **3.1 Signal channel MVP** (`4d2c51f`) — `SC_ENABLE_SIGNAL` (default n). New
  `src/channels/signal.{c,h}` + `signal_internal.h` (JSON-RPC over external
  signal-cli, polling receive, DM+group send, pure normalization/envelope seams).
  `test_signal.c` (18 cases). 🟠 **Live signal-cli smoke test = human gate.**

### Phase 4 (10 of 11 landed)
- **4.8 audit mediums** (`4046171`) — M-2 web pipe leak (`_Static_assert` for
  atomic pipe write) + M-4 session unbounded-growth backstop
  (`sc_session_force_prune_due`). M-5/M-6/M-7/M-10 verified already-closed (M-7
  regression test); M-9 deferred.
- **4.4 old-result compression config gate** (`3f3de6a`) — core already shipped
  (`mask_old_observations`); added `compress_tool_results`/`compress_keep_recent`/
  `compress_min_bytes` + pure `sc_mask_should_compress`.
- **4.7 prompt budget CLI** (`101eb3b`) — `smolclaw context [session] [--warn-pct N]`;
  pure `sc_context_estimate_tokens`/`sc_context_budget_warn` + `context_warn_pct`.
- **4.12 agent-initiated compact tool** (`c4ea772`) — `compact` tool +
  shared `sc_agent_compact_session` (slash `/compress` refactored to reuse) +
  cooldown/budget guards + per-turn `agent->active_session_key`.
- **4.2 capability-aware MCP sandbox** (`6bbc26f`) — **security:** rewrote
  `apply_seccomp` to build the BPF denylist at runtime; fixed the dead
  `no_process` cap (was parsed but never enforced) and added a `network` cap.
  4 fork-based `test_sandbox` probes verify caps block + defaults allow.
- **4.11 global session_search** (`8f95b3d`) — `SC_ENABLE_SESSION_SEARCH`
  (default n, depends on MEMORY_SEARCH); FTS5 over `sessions/*.jsonl` via
  `sc_memory_index_rebuild_dir`; lazy index; actions search/list.
- **4.10 updater split spike** (`bc18343`) — measurement only: updater = **8.0 KB
  stripped** (OpenSSL dynamic → no static weight) ≪ 50 KB → confirmed Q4 no-split.
- **4.13 post-turn memory review** (`ef58503`) — opt-in (default off); async
  `sc_task` proposes 0–2 entries from a turn digest. **Caught+fixed a bug**: the
  worker used `sc_memory_write_long_term` (overwrites MEMORY.md by design);
  added `sc_memory_append_long_term` (safe RMW). 🟠 Live LLM review = human gate.
- **4.14 staged memory writes** (`54c1f8f`) — `memory_write_approval` staging to
  `memory/pending/` + CLI `smolclaw memory pending|approve|reject` + Web
  `GET/POST /api/memory/pending` + capacity header + dedup
  (`sc_memory_is_duplicate`/`sc_memory_capacity_pct`).
- **4.5 project memory + repo_search** (`481a84e`) — `SC_ENABLE_PROJECT_MEMORY`
  (default n). New `src/project_memory.{c,h}` (JSON code index at
  `{SMOLCLAW_HOME}/indexes/<hash>.json`; per-file terms/symbols/imports;
  recursive walk + incremental reuse; pure hash/lang/tokenize/rank seams) +
  `src/tools/repo_search.c`. System-prompt injection deferred (owner scope).

## Validation

- Clean Release build from `configs/defconfig`: **KC-2 `implicit`=0, no errors**.
- `ctest` (default build): **50/50 passing**. Feature-gated test counts:
  +`test_signal` (SIGNAL), +`test_session_search` (SESSION_SEARCH),
  +`test_project_memory` (PROJECT_MEMORY), +web tests (WEB) when those flags are on.
- Size budget: minimal-dynamic **285 KB** stripped ≤ 1024 KB (grew from 269 KB
  over the session via always-compiled config plumbing + `memory_review.c`;
  flag-gated features add nothing to minimal).
- `check_claude_md.sh`: clean. KC-1 satisfied for every new flag
  (`SC_ENABLE_SIGNAL`, `SC_ENABLE_SESSION_SEARCH`, `SC_ENABLE_PROJECT_MEMORY`).

## Remaining work / next steps

- **4.1 arena adoption (READY, higher risk):** convert provider SSE/response
  parsing (`src/providers/http.c`, `src/providers/provider_common.c`, ~46
  malloc sites) to allocate from `agent->arena` so the unchecked-alloc class
  collapses to one OOM check point. The allocator + per-turn reset already ship;
  only provider-parse adoption remains. Hot-path refactor — do carefully.
- **GATED-EXT (human/external acceptance):** 4.3 Anthropic prompt caching (needs
  an Anthropic key), 4.6 `doctor --local` (live provider), 4.9 microsandbox exec
  (KVM host). Also Phase 2: 2.1/2.2 xAI OAuth, 2.12 MCP cookbook.
- **Human acceptance gates from this session:** 3.1 Signal (live signal-cli),
  4.13 memory review (live LLM).
- **Deferred follow-ups:** 4.5 system-prompt injection for code questions; 4.13
  cross-thread gateway notification publish; 3.3 interactive diff-preview / async
  summary-confirm; M-9 libevent thread-safety (needs a repro).
- **Phase 5:** do-not-build parking lot.
