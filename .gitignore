build/
build-*/
dist/
.config
.config.old
.claude/
*.o
*.a
deps/static/
deps/static-build/
deps/musl-static-*/
deps/musl-build-*/
deps/musl-toolchain-*/
deps/*.tgz
deps/musl-cross-make/
*.deb
*.log
.env
.env.*
code-analysis-report.md
CLAUDE.md
CLAUDE.md.bak
Testing/

# Host / editor noise that can appear at the repo root — never commit these
.bashrc
.bash_profile
.profile
.zshrc
.zprofile
.gitconfig
.mcp.json
.ripgreprc
.idea
.vscode

# Internal-only docs (kept local, never published to the public mirror)
docs/progress-*.md

# Security-audit artifacts (path dumps, recon) — kept local; REPORT.md files stay tracked
docs/security-audits/**/artifacts/
